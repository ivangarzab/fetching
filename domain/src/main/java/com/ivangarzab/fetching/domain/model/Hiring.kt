package com.ivangarzab.fetching.domain.model

/**
 * The Hiring data class represents a hiring record with an id, listId, and name fields.
 */
data class Hiring(
    val id: Int,
    val listId: Int,
    val name: String
)
